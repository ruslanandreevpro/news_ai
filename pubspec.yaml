name: news_ai
description: News AI Application

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.4
  get: ^4.3.8
  line_icons: ^2.0.1
  flutter_slider_drawer: ^2.0.0
  badges: ^2.0.1
  url_strategy: ^0.2.0
  appwrite: ^2.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.4

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/logos/
